services:
  - type: web
    name: stageready-omr
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    healthCheckPath: /health
    envVars:
      - key: ALLOWED_ORIGINS
        sync: false
